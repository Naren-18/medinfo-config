server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://ep-green-brook-at8hrajg-pooler.c-9.us-east-1.aws.neon.tech/audit-db?sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_gASOj1DN6Bha

  jpa:
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: localhost:9092

    consumer:
      group-id: audit-group
      auto-offset-reset: earliest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true